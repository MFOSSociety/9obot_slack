var router = require('express')
var request = require('request')

const route = router()
let features = [
    'list',
    'add'
]


route.get('/', async (req, res) => {
    
})


module.exports = route;
